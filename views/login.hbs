<!DOCTYPE html>
<html lang="en">
<head>
    {{> head}}
    <title>Money Journal - Login</title>
</head>
<body>
    <div class="center-container">
        <div class="md-card" style="width: 100%; max-width: 400px;">
            <div class="md-card-header text-center">
                <span class="material-symbols-outlined" style="font-size: 48px; color: var(--md-sys-color-primary);">account_balance_wallet</span>
                <h1 class="md-headline">Money Journal</h1>
                <p class="md-subhead">Sheilla & Gilang</p>
                
                {{#if error}}
                <div style="background-color: var(--md-sys-color-error-container); color: var(--md-sys-color-on-error-container); padding: 8px; border-radius: 4px; margin-top: 16px; font-size: 14px;">
                    {{error}}
                </div>
                {{/if}}

                {{#if success}}
                <div style="background-color: var(--md-sys-color-secondary-container); color: var(--md-sys-color-on-secondary-container); padding: 8px; border-radius: 4px; margin-top: 16px; font-size: 14px;">
                    {{success}}
                </div>
                {{/if}}
            </div>
            
            <form method="POST" action="/auth/login">
                <div class="md-text-field">
                    <input type="text" id="username" name="username" placeholder=" " required>
                    <label for="username">Username</label>
                </div>
                
                <div class="md-text-field">
                    <input type="password" id="password" name="password" placeholder=" " required>
                    <label for="password">Password</label>
                </div>
                
                <button type="submit" class="md-btn filled md-btn-block">
                    Login
                </button>
            </form>

            <div class="text-center mt-4">
                <p class="md-subhead">Don't have an account?</p>
                <a href="/register" class="md-btn text">Create Account</a>
            </div>
        </div>
    </div>
</body>
</html>